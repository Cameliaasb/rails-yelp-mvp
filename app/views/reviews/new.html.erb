<div class="container">

  <h1 class="m-3">Leave a review </h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: (1..5) %>
    <%= f.button :submit, class:"btn btn-primary" %>
  <% end %>

</div>
